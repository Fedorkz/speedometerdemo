apply plugin: 'com.android.application'

android {
    compileSdkVersion 11
    buildToolsVersion "23.0.1"

    defaultConfig {
        applicationId "com.fedorkzsoft.speedometer"
        minSdkVersion 11
        targetSdkVersion 11
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}
